import { OccupiedSlotsPipe } from './occupied-slots.pipe';

describe('OccupiedSlotsPipe', () => {
  it('create an instance', () => {
    const pipe = new OccupiedSlotsPipe();
    expect(pipe).toBeTruthy();
  });
});
