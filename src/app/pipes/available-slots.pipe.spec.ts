import { AvailableSlotsPipe } from './available-slots.pipe';

describe('AvailableSlotsPipe', () => {
  it('create an instance', () => {
    const pipe = new AvailableSlotsPipe();
    expect(pipe).toBeTruthy();
  });
});
